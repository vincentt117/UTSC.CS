package sprites;

import game.Constants;

/**
 * A class that represents a clean hall way of the vacuum game.
 */

public class CleanHallway extends Sprite {
  /**
   * Creates a new <code>CleanHallway</code> that represents a CleanHallway in
   * the game.
   * 
   * @param row
   *          the row which this <code>CleanHallway</code> resides in now
   * @param column
   *          the column which this <code>CleanHallway</code> resides in now
   */
  public CleanHallway(int row, int column) {
    super(Constants.CLEAN, row, column);
    // TODO Auto-generated constructor stub
  }

}