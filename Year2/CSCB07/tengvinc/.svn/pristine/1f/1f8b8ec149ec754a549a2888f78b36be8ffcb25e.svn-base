package sprites;

import game.Constants;

/**
 * A class that represents the walls of the vacuum game.
 */

public class Wall extends Sprite {
  /**
   * Creates a new <code>Wall</code> that represents a Wall in the game.
   * 
   * @param row
   *          the row which this <code>Wall</code> resides in now
   * @param column
   *          the column which this <code>Wall</code> resides in now
   */
  public Wall(int row, int column) {
    super(Constants.WALL, row, column);
    // TODO Auto-generated constructor stub
  }

}
