package sprites;

import game.Constants;

/**
 * A class that represents a dumpster of the vacuum game.
 */

public class Dumpster extends Sprite {
  /**
   * Creates a new <code>Dumpster</code> that represents a Dumpster in the game.
   * 
   * @param row
   *          the row which this <code>Dumpster</code> resides in now
   * @param column
   *          the column which this <code>Dumpster</code> resides in now
   */
  public Dumpster(int row, int column) {
    super(Constants.DUMPSTER, row, column);
    // TODO Auto-generated constructor stub
  }

}
